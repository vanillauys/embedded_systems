# Concepts

Standalone notes on Rust or embedded concepts that come up during the build. Each file is one focused idea, linked from the step(s) where it first mattered.

_Empty for now. Files will land here as concepts surface — e.g. `rust-ownership-vs-java.md`, `peripherals-singleton.md`, `one-wire-protocol.md`, `rtc-memory-and-deep-sleep.md`._
